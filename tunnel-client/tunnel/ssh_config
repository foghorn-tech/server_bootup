Host Tunnel
  ServerAliveInterval 30
  ServerAliveCountMax 10

  ControlMaster auto
  ControlPath /tmp/ssh_mux_%h_%p_%r.socket
  ControlPersist 0

  HostName tunnel.kanaries.cn
  User tunnel
  Port 22
  